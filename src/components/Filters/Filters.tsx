import React, { memo } from 'react'

export const Filters: React.FC = memo(() => {
  return (
    <div>
      <div style={{ height: 30, width: 50, color: 'black', border: '1px solid grey' }}>Add</div>
    </div>
  )
})
